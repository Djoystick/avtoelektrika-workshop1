name: 🔄 Обновить базу автоэлектрики

on:
  # Запуск по расписанию - каждые 6 часов
  schedule:
    - cron: '0 */6 * * *'
  
  # Ручной запуск
  workflow_dispatch:
  
  # Запуск при push в main
  push:
    branches:
      - main
    paths:
      - 'scripts/**'
      - '.github/workflows/update.yml'

jobs:
  parse-and-update:
    runs-on: ubuntu-latest
    name: Парсинг новостей
    
    steps:
      # Клонируем репозиторий
      - name: 📥 Клонирование кода
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      
      # Настраиваем Python
      - name: 🐍 Установка Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      # Устанавливаем зависимости
      - name: 📦 Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      # Запускаем парсер
      - name: 🔧 Парсинг новостей
        run: |
          cd scripts
          python fetch_news.py
        continue-on-error: true
      
      # Проверяем, что файл создан
      - name: ✅ Проверка news.json
        run: |
          if [ -f news.json ]; then
            echo "✅ news.json создан"
            du -sh news.json
          else
            echo "❌ news.json не создан"
            exit 1
          fi
      
      # Коммитим обновления
      - name: 📝 Коммит обновлений
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add news.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 Обновлена база автоэлектрики — $(date '+%Y-%m-%d %H:%M:%S')" && git push)
      
      # Логируем результат
      - name: 📊 Логирование результата
        if: always()
        run: |
          echo "✅ Workflow завершён в $(date)"
          echo "📍 Коммит: ${{ github.sha }}"
