name: 🔄 Обновить базу автоэлектрики (каждые 30 минут)

on:
  # Запуск каждые 30 минут
  schedule:
    - cron: '*/30 * * * *'
  
  # Ручной запуск
  workflow_dispatch:
  
  # Запуск при изменениях в scripts
  push:
    branches:
      - main
    paths:
      - 'scripts/**'
      - '.github/workflows/update.yml'

permissions:
  contents: write
  actions: read

jobs:
  parse-and-update:
    runs-on: ubuntu-latest
    name: 🔧 Парсинг 100+ источников
    timeout-minutes: 30
    
    steps:
      - name: 📥 Клонирование кода
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      
      - name: 🐍 Установка Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 🔧 Парсинг новостей из 100+ источников
        run: |
          cd scripts
          python fetch_news.py
        continue-on-error: true
      
      - name: ✅ Проверка news.json
        run: |
          if [ -f news.json ]; then
            echo "✅ news.json создан успешно"
            du -sh news.json
            head -c 200 news.json
          else
            echo "❌ news.json не создан"
            exit 1
          fi
      
      - name: 📝 Коммит обновлений
  run: |
    git config --local user.email "github-actions[bot]@users.noreply.github.com"
    git config --local user.name "github-actions[bot]"
    git add news.json
    git diff --quiet && git diff --staged --quiet || (
      git pull --rebase origin main
      git commit -m "🔄 Обновлена база (100+ источников) — $(date '+%Y-%m-%d %H:%M:%S')"
      git push
    )

      
      - name: 📊 Статистика
        if: always()
        run: |
          echo "✅ Workflow завершён: $(date)"
          echo "📍 Коммит: ${{ github.sha }}"
          echo "⏰ Следующий запуск через 30 минут"
